import * as React from "react";
import './Header.scss';

export const Header = ({}) => {
  return(
    <div className='headerPrimary'>
      <h2 className="headerText">
        Dignisia Task
      </h2>
    </div>
  )
};

export default Header;
